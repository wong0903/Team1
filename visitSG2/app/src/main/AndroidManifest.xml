<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wong0903.visitsg">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="boundary.SearchInterface"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:theme="@style/FullscreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="boundary.LoginInterface"
            android:label="Login"
            android:theme = "@style/FullscreenTheme">
        </activity>

        <activity android:name="boundary.SignUpInterface"
            android:label="Registration"
            android:theme = "@style/FullscreenTheme">
        </activity>

        <activity android:name="boundary.ViewInterface"
            android:label="Registration"
            android:theme = "@style/FullscreenTheme">
        </activity>

        <activity android:name="boundary.CategoryInterface"
            android:label="Registration"
            android:theme = "@style/FullscreenTheme">
        </activity>


    </application>

</manifest>